# Libraries

Audience: Both

## Overview
Shared libraries and schemas used across services. Requirements are centralized in [libs/requirements.txt](libs/requirements.txt).

## Development
- Install: `pip install -r libs/requirements.txt`
- Testing: placeholder (`pytest`)
- Linting: placeholder (`ruff`/`flake8`)

## Versioning policy
Semantic Versioning (SemVer): MAJOR.MINOR.PATCH for library artifacts that may be published independently in the future.
